---
name: roi-enlighten
description: Detect reusable patterns and propose a human-gated capability.
---

Use `enlighten_run` (logical `enlighten.run` / `roi:enlighten`) after verified work or repeated work patterns. Report the
detected pattern and the proposed capability, but do not promote
automatically.
