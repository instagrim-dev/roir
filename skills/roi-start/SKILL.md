---
name: roi-start
description: Start a new ROI mission and seed the first brief.
---

Use `mission_create` (MCP tool `name`; logical id `mission.create`) to create the mission, then summarize the new mission ID,
goal, and next action.
